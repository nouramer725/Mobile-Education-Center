import 'package:flutter/material.dart';

class Container1Press extends StatelessWidget {
  const Container1Press({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        // title: const Text('Container 1 Press'),
      ),
    );
  }
}
