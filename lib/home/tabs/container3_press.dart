import 'package:flutter/material.dart';

class Container3Press extends StatelessWidget {
  const Container3Press({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        // title: const Text('Container 3 Press'),
      ),
    );
  }
}
