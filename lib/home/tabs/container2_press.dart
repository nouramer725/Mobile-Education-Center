import 'package:flutter/material.dart';

class Container2Press extends StatelessWidget {
  const Container2Press({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        // title: const Text('Container 2 Press'),
      ),
    );
  }
}
